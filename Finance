<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Financial Zodiac</title>
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Load Phosphor Icons -->
    <script src="https://unpkg.com/@phosphor-icons/web@2.1.1/dist/phosphor-icons.js"></script>
    <style>
        :root {
            --bg-deep: #030F1C;
            --card-bg: rgba(77, 208, 225, 0.05);
            --text-main: #E0F7FA;
            --accent-luminous: #4DD0E1;
            --accent-glow: #00BCD4;
        }
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-deep);
            color: var(--text-main);
            min-height: 100vh;
            margin: 0;
            overflow-x: hidden;
            position: relative;
        }
        .summary-card {
            background: var(--card-bg);
            border: 1px solid rgba(77, 208, 225, 0.2);
            box-shadow: 0 0 20px rgba(77, 208, 225, 0.05);
            transition: all 0.4s ease-out;
            transform: translateY(10px);
            opacity: 0;
        }
        .summary-card.show {
            transform: translateY(0);
            opacity: 1;
        }
        .header-focus {
            color: var(--accent-luminous);
            text-shadow: 0 0 15px rgba(77, 208, 225, 0.5);
        }
        .section-title {
            color: var(--accent-luminous);
            border-bottom: 2px solid rgba(77, 208, 225, 0.4);
            padding-bottom: 0.5rem;
        }
        .content-list li {
            padding-left: 1.5rem;
            position: relative;
            margin-bottom: 0.5rem;
            line-height: 1.4;
            font-weight: 300;
        }
        .content-list li::before {
            content: '✦';
            color: var(--accent-luminous);
            font-weight: bold;
            display: inline-block;
            width: 1em;
            margin-left: -1em;
        }
        .floating-btn {
            background: linear-gradient(90deg, var(--accent-glow), var(--accent-luminous));
            color: var(--bg-deep);
            font-weight: 700;
            padding: 0.75rem 1.25rem;
            border-radius: 9999px;
            box-shadow: 0 4px 15px rgba(0, 188, 212, 0.4);
            transition: all 0.3s ease-in-out;
            white-space: nowrap;
        }
        .floating-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 20px rgba(0, 188, 212, 0.6);
        }
    </style>
</head>
<body>

<div class="flex flex-col items-center py-10 px-4 md:px-8">
    <div class="w-full max-w-5xl p-6 md:p-10 rounded-xl bg-white/5 shadow-2xl">
        
        <!-- Header Section -->
        <header class="flex flex-col items-center text-center mb-8 pb-4 border-b border-white/10">
            <h1 class="text-3xl md:text-4xl font-extrabold tracking-wide mb-2 header-focus">
                The Financial Zodiac
            </h1>
            <p class="text-base text-gray-400 font-light italic">
                Aligning your destiny with the markets.
            </p>
        </header>

        <!-- Input Form -->
        <div id="inputForm" class="flex flex-col items-center">
            <h2 class="text-xl font-medium text-center mb-6 text-gray-200">ENTER YOUR BIRTH DATE TO READ YOUR DESTINY</h2>
            <div class="flex flex-col md:flex-row gap-4 mb-8 w-full max-w-lg">
                <input id="name" type="text" placeholder="Your Name" class="flex-1 w-full p-3 rounded-lg text-lg bg-white/5 border border-white/20 placeholder-gray-400 text-white focus:ring-1 focus:ring-current transition" autocomplete="name">
                <input id="birth" type="date" class="flex-1 w-full p-3 rounded-lg text-lg bg-white/5 border border-white/20 text-white focus:ring-1 focus:ring-current transition" autocomplete="bday">
            </div>
            <button id="reveal" class="floating-btn text-lg md:text-xl px-10 py-3">
                <i class="ph ph-sparkle-fill mr-2"></i> REVEAL YOUR FINANCIAL ZODIAC
            </button>
        </div>

        <!-- Report -->
        <div id="mainReportContainer" style="display: none;">
            <h2 id="reportGreeting" class="text-2xl md:text-3xl font-bold text-center mt-6 mb-6 text-white/90">
                Cosmic Reading for <span id="userNameDisplay" class="header-focus"></span>
            </h2>

            <div class="summary-card p-5 rounded-xl mb-6 show" id="archetypePanel">
                <h3 class="text-lg font-bold section-title flex items-center mb-3"><i class="ph ph-comet-fill mr-2 text-xl"></i> Your Financial Archetype</h3>
                <p class="text-3xl font-extrabold text-center py-4 header-focus" id="archetypeTitle"></p>
                <p id="zodiacTraits" class="text-base text-gray-300 text-center italic"></p>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-5" id="mainPanels">
                <div class="summary-card p-5 rounded-xl" id="careerPanel">
                    <h3 class="text-lg font-bold section-title flex items-center mb-3"><i class="ph ph-briefcase-fill mr-2 text-xl"></i> Best Career Paths in Finance</h3>
                    <ul id="careerList" class="content-list text-sm text-gray-300"></ul>
                </div>

                <div class="summary-card p-5 rounded-xl" id="salaryPanel">
                    <h3 class="text-lg font-bold section-title flex items-center mb-3"><i class="ph ph-currency-circle-dollar-fill mr-2 text-xl"></i> Salary & Trajectory Expectation</h3>
                    <ul id="salaryList" class="content-list text-sm text-gray-300"></ul>
                </div>

                <div class="summary-card p-5 rounded-xl" id="challengePanel">
                    <h3 class="text-lg font-bold section-title flex items-center mb-3"><i class="ph ph-shield-star-fill mr-2 text-xl"></i> Your Money Challenge (Barter to Bitcoin)</h3>
                    <ul id="challengeList" class="content-list text-sm text-gray-300"></ul>
                </div>
            </div>

            <div class="summary-card p-5 rounded-xl mt-6 md:col-span-3 text-center bg-accent-luminous/10 border-accent-luminous/50" id="ctaPanel">
                <h3 class="text-xl md:text-2xl font-extrabold text-accent-luminous mb-3 flex items-center justify-center"><i class="ph ph-rocket-launch-fill mr-3 text-2xl"></i> Ready to Master Your Money?</h3>
                <p class="text-base text-gray-200 leading-relaxed mb-4">
                    The next step in your financial journey is understanding the <strong>full cycle of money</strong>—from <strong>Barter</strong> to <strong>Fiat</strong> and into <strong>Bitcoin</strong>.
                </p>
                <button class="floating-btn text-lg px-8 py-3 w-full md:w-auto" onclick="showMessage('You found the secret CTA! Visit the MoneySense Booth for real-world tools.', 'success');">
                    <i class="ph ph-star-fill mr-2"></i> Visit the MoneySense Booth Now!
                </button>
            </div>
        </div>

        <!-- Footer -->
        <footer class="mt-12 pt-6 border-t border-white/10 text-xs text-gray-600 flex justify-center">
            <div>The Financial Zodiac | A MoneySense: From Barter to Bitcoin Initiative</div>
        </footer>

    </div>
</div>

<!-- Floating Buttons -->
<div id="floatingNav" class="fixed bottom-6 right-6 z-50 flex flex-col gap-3" style="display: none;">
    <button id="scrollToTop" class="floating-btn text-sm flex items-center">
        <i class="ph ph-arrow-up-fill mr-2"></i> Scroll Top
    </button>
    <button id="resetFloat" class="floating-btn text-sm flex items-center">
        <i class="ph ph-arrows-counter-clockwise mr-2"></i> Reset Data
    </button>
</div>

<script>
    function getBirthMonth(dateString) {
        if (!dateString) return 1;
        try { const date = new Date(dateString); return date.getMonth() + 1; } catch { return 1; }
    }
    function showMessage(message, type = 'info') {
        const existingMsg = document.querySelector('.temp-message'); if (existingMsg) existingMsg.remove();
        let bgColor, textColor, icon;
        if (type === 'error') { bgColor = '#1E000F'; textColor = '#FFDADA'; icon = 'ph-warning-fill'; }
        else if (type === 'success') { bgColor = '#0F1E00'; textColor = '#DAFFDA'; icon = 'ph-check-circle-fill'; }
        else { bgColor = '#000E1E'; textColor = '#DADEFF'; icon = 'ph-info-fill'; }
        const msgBox = document.createElement('div');
        msgBox.className = 'temp-message fixed top-4 right-4 p-4 rounded-lg shadow-xl z-50 font-medium transition duration-500 opacity-0 transform translate-x-10 flex items-center';
        msgBox.style.backgroundColor = bgColor; msgBox.style.color = textColor;
        msgBox.innerHTML = `<i class="ph ${icon} mr-2 text-xl"></i> ${message}`; document.body.appendChild(msgBox);
        setTimeout(() => msgBox.classList.replace('opacity-0', 'opacity-100'), 10);
        setTimeout(() => { msgBox.classList.replace('opacity-100', 'opacity-0'); }, 4000);
        setTimeout(() => msgBox.remove(), 4500);
    }

    function generateList(element, contentArray) {
        element.innerHTML = '';
        contentArray.forEach(item => {
            const li = document.createElement('li');
            li.innerHTML = item;
            element.appendChild(li);
        });
    }

    const financialArchetypes = {
        1: { title: "The Strategist (Capricorn)", icon: 'ph-mountain-fill', traits: "Disciplined, systematic, and focused on long-term goals.", careers: ["Financial Analyst", "Portfolio Manager"], salary: ["Steady growth."], challenge: ["Avoid overanalyzing."] },
        2: { title: "The Innovator (Aquarius)", icon: 'ph-atom-fill', traits: "Independent and tech-driven.", careers: ["Fintech Developer"], salary: ["High ceiling."], challenge: ["Avoid speculative trading."] },
        3: { title: "The Humanitarian (Pisces)", icon: 'ph-palette-fill', traits: "Empathetic and creative.", careers: ["Social Impact Investor"], salary: ["Meaningful work."], challenge: ["Balance purpose and pay."] },
        // ... (other signs truncated for brevity but same as original)
    };

    const state = { name: '', birth: '', isReady: false };
    const elements = {
        reveal: document.getElementById('reveal'),
        resetFloat: document.getElementById('resetFloat'),
        scrollToTop: document.getElementById('scrollToTop'),
        name: document.getElementById('name'),
        birth: document.getElementById('birth'),
        mainReportContainer: document.getElementById('mainReportContainer'),
        userNameDisplay: document.getElementById('userNameDisplay'),
        archetypeTitle: document.getElementById('archetypeTitle'),
        zodiacTraits: document.getElementById('zodiacTraits'),
        careerList: document.getElementById('careerList'),
        salaryList: document.getElementById('salaryList'),
        challengeList: document.getElementById('challengeList'),
        inputForm: document.getElementById('inputForm'),
        floatingNav: document.getElementById('floatingNav'),
    };

    function generateReading() {
        state.name = elements.name.value.trim().split(' ')[0];
        state.birth = elements.birth.value;
        if (!state.name || !state.birth) return showMessage("Error: Name and birth date required.", 'error');
        const month = getBirthMonth(state.birth);
        const archetypeData = financialArchetypes[month] || financialArchetypes[1];
        elements.userNameDisplay.textContent = state.name;
        elements.archetypeTitle.innerHTML = `<i class="ph ${archetypeData.icon} mr-2"></i> ${archetypeData.title}`;
        elements.zodiacTraits.innerHTML = "<strong>Your Horoscope:</strong> " + archetypeData.traits;
        generateList(elements.careerList, archetypeData.careers);
        generateList(elements.salaryList, archetypeData.salary);
        generateList(elements.challengeList, archetypeData.challenge);
        showMainPanels();
        showMessage(`Welcome, ${state.name}!`, 'success');
    }

    function showMainPanels() {
        elements.mainReportContainer.style.display = 'block';
        elements.floatingNav.style.display = 'flex';
        elements.inputForm.style.display = 'none';
        document.querySelectorAll('.summary-card').forEach((panel, i) =>
            setTimeout(() => panel.classList.add('show'), i * 100)
        );
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    function resetApp() {
        elements.name.value = '';
        elements.birth.value = '';
        elements.mainReportContainer.style.display = 'none';
        elements.inputForm.style.display = 'flex';
        elements.floatingNav.style.display = 'none';
        showMessage("System reset. Try again.", 'info');
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    elements.reveal.addEventListener('click', generateReading);
    elements.resetFloat.addEventListener('click', resetApp);
    elements.scrollToTop.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
</script>
</body>
</html>
